getListCount=SELECT COUNT(*) FROM FAQ WHERE FAQ_STATUS='Y'
selectList=SELECT * FROM (SELECT ROWNUM RNUM, FAQ_NO, FAQ_CLASS, FAQ_TITLE, FAQ_CONTENT, FAQ_CREATEDATE FROM ( SELECT * FROM FAQ WHERE FAQ_STATUS = 'Y' ORDER BY FAQ_NO DESC ) ) WHERE RNUM BETWEEN ? AND ?
getSubListCount=SELECT COUNT(*) FROM FAQ WHERE FAQ_CLASS = ?
faqSubList=SELECT * FROM (SELECT ROWNUM RNUM, FAQ_NO, FAQ_CLASS, FAQ_TITLE, FAQ_CONTENT, FAQ_CREATEDATE FROM ( SELECT * FROM FAQ WHERE FAQ_STATUS = 'Y' AND FAQ_CLASS = ? ORDER BY FAQ_NO DESC ) ) WHERE RNUM BETWEEN ? AND ?

searchCount=SELECT COUNT(*) FROM FAQ WHERE FAQ_STATUS='Y' AND FAQ_TITLE LIKE ?
searchList=SELECT * FROM (SELECT ROWNUM RNUM, FAQ_NO, FAQ_CLASS, FAQ_TITLE, FAQ_CONTENT, FAQ_CREATEDATE FROM ( SELECT * FROM FAQ WHERE FAQ_STATUS = 'Y' AND FAQ_TITLE LIKE ? ORDER BY FAQ_NO DESC ) ) WHERE RNUM BETWEEN ? AND ?